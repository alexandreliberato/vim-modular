# Copilot Instructions
- Always check if the current change is not going to impact other functions and features already working
- Always try to use the simpler solution, for example an configuration that a plugin has instead creating new functions
- Do not change existing configurations and functions if you not sure that that change will not brake other things that use it
